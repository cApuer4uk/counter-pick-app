[pytest]
addopts = -q
testpaths = counterpick/tests

markers =
    slow: медленные/тяжёлые тесты (например, загрузка YOLO)
    integration: требует запуска внешних .exe
    smoke: быстрые проверки запуска exe
